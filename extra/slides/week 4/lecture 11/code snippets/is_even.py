def is_even(i):
    """
    Input: i, a positive integer
    Return True if i is even, otherwise return False.
    """
    print("Inside is_even()")
    return i % 2 == 0   # with return
    # i % 2 == 0          # without return


is_even(3)
